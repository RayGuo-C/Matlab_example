% we can type directly the myRand to realize the funtion
% myRand
%where a is local variable which is defined only in the funtion myRand, and it has no effect on
% the cammand window workspace. print a, it will show 'Undefined function or variable 'a'.
a
myRand

% function output
% where a is local variable which is defined only in the funtion myRand, and it has no effect on
% the cammand window workspace. print a, it will show 'Undefined function or variable 'a'.
% to identify where the user-defined function can output into a variable
% b = myRand
% b
% a

% function input
% apply parameters to the functions
% myRand(5,10)
% or: x = 0.5, y=1, z = myRand(x,y)

% multiple Output
% myRand(1,2)
% x = myRand(-1,1)
% [x,y] = myRand(0,1)

% subfunction
% myRand(-5,5)
% s
% a